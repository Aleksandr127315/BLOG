Начало Работ

1 урок
установили пакеты которые нам нужны(redux,react-redux,redux-thunk,react-hook-form,styled-components,react-router,yup,prop-types и json-server global)
преобразовали index.html
подчистили все файлы удалили лишние css файлы так как будем рабоать с styled-components и дали первый стиль для первого блочного элемента Div

---

урок 2
скачали иконочный шрифт,закинули в проект
в папке index.css импортировали то что мы закинули в проект,важно что в самом файле font-awesome.css изменили путь к файлам так как изменили название папки
познакомились с сайтом fontawesome потренировались с иконками(то зачем мы всё скачали)
сделали и закометили ветку с помощь git(BLOG-003)

---

урок 3
области хранения данных:
-база данных на json-server
-BFF
-редакс стор

сущности и приложение:
-пользователь : БД(список пользователей),BFF(ссесия текущего),стор(отображение в браузере)
-роль пользователя: БД(список ролей),BFF(сессия пользователя),стор(использование на клиенте)
-статья:БД(список статей),стор(отображение в браузере)
-коментарии:БД(список комментариев),стор(отображение в браузере)

таблицы БД:

- пользователи - users: id / login / password / registed_at / role_id
- роли - roles: id / name
  -статьи - posts: id / title / image_url / content / published_at
  -комметарии - comments: id / author_id / post_id / content

---

урок 4
Схема состояния на BFF:
-сессия текущего пользователя: login / password / role

Схема для редакс стора (на клиенте):
-user: id / login / roleId / session
-posts: массив post: id / title / imageUrl / content / publishedAt
-post: id / title /imageUrl / content / publishedAt / comments: массив comment: id / author / content / publishedAt
-users: массив user: id / login / registeredAt / role

---

урок 5
посмотрели наброски в photopea и зашли на сайт рыбаТекст познакомились с приложением генератор текста , прикинули как будет выглядить сайт

---

урок 6
далее начинаем заполнять нашу базу данных которую мы создали в папке src db.json и начинаем прописывать там информацию , сделали и заполнили db.json (сделали 4 массива(users,roles,posts,comments) и запустили server ответы он присылает на хоте 3005)

---

урок 7
начали первично создавать BFF(бэкенд для фронтенда) реализуем его локально , создали папку bff и файл bff.js внутри создаём метод authorize который принимает логин и пароль с помощью fetch запроса с ссылкой на базу данных шашей db.json , создали переменую которая нам вернём массив users, далее с помощью метода find ищем совпадение логинов. делаем проверку с помощью if , если ответ не получен то выводим ошибку error с текстом и res:null . Далее проверяем чтобы пароли совподали с помощью if если наш authPassword !== users.password возвращаем error и res:null , если логин и пароль совпали возвращаем error:null и res(например removeComment logout(разлогинится) для этого мы берём ключи Object.keys(session) проходимся с помощью forEach((key) => { delete session[key]}) удаляем все ключи).

---

урокм 8
далее реализовываем первичную реализацию bff(регистрация) , но только уже для пользователя который регистрируется , так же делаем запрос на сервер , он возвращает ответ и тут мы уже проверяем еслит наш user есть , то возвращаем ошибку , двигаемся дальше с помощью method:'POST' добавляем полтзователя со всеми данными на сервер с ролью читатель и для даты регистрации мы добавили случайную генерацию числе , далее так же подключаем session и уже возвращаем error:null и res:session

---

урок 9
дальше создаём первичную реализацию bff(рефакторинг.сессии)наводим порядок в коде

1. создали get-users(запрос пользователей от базы данных)
2. generate-date переместили в отдельный файл и вызываем там где это необходимо
3. далее создаём get-user в этот файл мы импортируем get-users в этом компанете мы вызываем async,aweit так как мы получааем много операций и далее мы возвращаем одного пользователя
4. дальше разгружаем наш основной компонент и уже там вызываем функцию getUser с перевым аргументом который будет authLogin для авторизадции и regLogin для регистрации
5. создаём add-user в этот файл импортируем generateDate и в аргументы нашей функции передаём login и password (в body мы передаём login и password как обычные значения)
6. в нашем основном компоненте вызываем функцию add-user и передаём в неё regLogin и regPassword
7. создаём create-session в аргументы принимаем roleId , создаём объект session и передаём в него logout ,далее с помощью switch/case по roleId мы раздаём права какие могут быть
8. создаём папку session и в ней созд компонент removeComment и дальше его импортируем в папке create-session
9. далее выносим roleId в константы для этого создаём папку constants и в ней файл role с пояснением какая цифра какой роли ровняется,гостя не указываем и так же индексируем
10. далее импортруем наш объект ROLE и в switch/case указываем роли и заготавливаем для 3х ролей права для удаления админ может, модератор тоже , читатель нет
11. в нашем bff в res передаём createSession(user.role_id) как указано на нашем сервере
12. закомитиил наши изменения(создали bff)

---

укро 10

1. начали реализовывать основной компонент блога(переименуем его в Blog) и сам компонент переименовываем и так же в импорте тоже
2. зададим пустые Herader и Footer они будут неизменные
3. далее будем работать с routing , импортируем BrowserRouter оборачиваем наш Blog
4. импортируем Routes и Route вставляем в проект и у нас будет 8 страниц (главная,авторизация,регистрация,пользователи,статья,новая статья,ошибка(у нас их две будет))
5. в styled content передаём padding 120px
6. для styled h2 задаём центровку

---

урок 11

1. начинаем рализацию шапку странцы
2. в основном css файле задаём цвет фона #405060
3. для html body и #root задали высоту 100%
4. возьмём наш основной компонент и стелизуем его в компонент AppColumn
5. начинаем работать с шапкой т.к. у нас целый компонент синтаксис стилизации чуть отличаеться styled(Header)` и далее прописываем стили и используем его уже в разметке , так же в наш компонент в пропсы передаём объект с className и в разметке передаём className привычным способом проверяем это всё дело в DevTools
6. ищем в fontawesome нашу иконку котрую мы уже продумали до этого и копируем её </> наш логотип
7. создаём папку components также index.js
8. так же создаём папку header в самом компоненте уже стизизуем наш компонент header(проверяем в браузере чтобы всё работало)
9. в папке header создаём папку components и добавляем папку logo и создаём файл logo.jsx так же стилизуем этот компонент в этом компоненте добавляем так же 1.Блог 2.веб-разработчика и так же их стилизуем
10. подключаем наш Logo в файле header(и проверяем в браузере)
11. стилизуем наши компоненты в logo.jsx можно это делать черрез инструменты разработчика задаём все отступы и добавляем размеры
12. в самом header задаём отступы padding
13. в общем css применяем ко всем тегам box-sizing: border-box
14. изза того что наш логотип съехал в компоненте logo делаем отризацельный margin-top: -14px
15. далее делаем тень для нашего header и фиксируем его к верху через position:fixed и top:0 и также зададим ширину которую мы до этого обозначили 1000px и для того чтобы крыть контент который должен уезжать под шапку , делаем белый фон

---

урок 12

1. дальше делаем вторую колонку в шапе Discription применяем стили к Header display:flex и justify-content:space-between чтобы растянуть контент вдоль всего header
2. применяем font-stile:italic
3. делаем компонент для кнопок ControlPanel
4. создаём ControlPanelContainer в пропсы принимаем объект с className и разметка
5. далее создаём компонент RightAligned(внутри которого уже будут кнопки) и добаляем в него стили и добавляем его в наш ControlPanelContainer
6. не забываем его экспортировать через index.js
7. вставляем наш элемент ControlPanel после Discription
8. Делаем обший копонент icon.jsx для того чтобы его переиспользовать
9. в пропсы принимае id и выводим запись уже в компоненте также в styledComponent через функцию в которой мы принимаем пропсы выводим размер и отступы и изначально зададим им значения size = '24px' margin = '0'
10. подключаем в лого на новый компоненнт Icon принимаем в пропсы(fa-code font-size(70px) и margin(0 10px 0 0))
11. в ControlPAnel подключаем Icon 3 наших иконки по примеру с logo
12. увеличиваем кнопку войти через стилизацию

---

урок 13

1. реализовываем функционал шапки , то есть всех кнопок
2. импортируем Link из react-router-dom
3. в LogoContainer заменяем div на Link и to="/" переход на главную страницу
4. в основном компоненте css для ссылок прописываем color:#000 и text-decoration: none , чтобы текст был черным и не было подчёркиваний
5. в компонент ConrtolPanel так импоритируем Link и кнопку button мы меняем на ссылку делаем ей border: 1px solid #000(рамку), центруем текст в рамке с помощью display:flex, justify-content:center, align-items:center(но можно было обернуть саму кнопку в Link) и добавляем цвет на фон в to="/login"
6. для кнопок новая статью и пользователи всё достаточно просто оборачиваем их в Link и прописываем в to="/post" для новая статьтся и to="/users" для пользователей
7. для кнопки назад импортируем хук useNavigate, на верхнем уровне компонента мы создаём переменную navigate , так как onClick нужно передавать в обёртку Icon то есть в родительском компоненте мы нашу обёртку это будет div назавём её StyledButton и стилизуем его &hover{ cursor:pointer;} в onClick={()=>navigate(-1)} передаём логику назад
8. и поднимем чуть чучть наш логотип margin-top:-21px(это легче делать в инструментах разработчика)

---

урок 14

1. начинаем реализовывать футер
2. начнём с самого сложного реализуем виджет погоды(можем это сделать с помощью яндекса с помощью api-ключа на сайте API Яндекс.Погода)
3. с помощью fetch пытаемся отправить запрос из консоли
4. fetch(url,headers:{'Content-Type':'aplication/json',X-Yandex-API-Key:'какой-то ключ'}).then((data)=>data.json()) и всё это выводим в консоль
5. по CORS(сервис против хакеров) политике запрос не пришёл так как яндекс не даёт с локального сервера и яндекс блокирует запрос , альтернативный выход размещать этот запрос на сервере , не наш вариант так как у нас сервер на фронтенде
6. мы будем использовать Open Weather
7. регестрируемся, создаём ключ , и в документации смотрим какую ручку(адрес запроса) мы будем создавать
8. проверяем это всё через консоль прямов браузере

---

урок 15

1. создаём компонент Footer асинхронные запросы делаем в специальном хуке useEffect
2. в хуке используем наш запрос fetch(и обрабатываем их с помощью json и принимаем объект с данными(name,main,weather) для вывода данных)
3. создаём 3 состояния(city,temperature,weather) и в хуке useEffect в then(через set-функции устанавливаем наши значения полученные с ручки)
4. не забываем эспортировать в index.js наш footer
5. начинаем верстать(всё оборачиваем в обёртки слева мы пишем блог веб-разработчика и почту указываем)
6. справа мы выводим температуру, описание погоды и название города , так же в нашей родительской обёртке div на верхнем уровне footer добавяем className={className} а принимаем его из аргументов FooterContainer для того чтобы мы могли его стилизовать
7. дальше стилизуем наш компонент с помощью styledComponet
8. задаём display:felx,justify-content:space-between,align-items:center для того чтобы контент в нашем footer был распределён по разным сторонам слева и справа
9. так в инструментах разработчика мы смотрим как мы делали шапку(высоту,ширину,падинги,цвет фона и тень от рамки,так же добавим жирный текст(width: 1000px;height: 120px;padding: 20px 40px;font-weight: bold;background-color: #fff;box-shadow: 0 2px 17px #000;)в шапке мы смещали на -2px а в footer мы её смещаем на 2px чтобы тень выглядела одинаково)
10. так же справа выводим текущаю дату, там где мы указывали город {new Date().toLocaleString('ru', { day: 'numeric', month: 'long' })}

---

---

---

Вход и регистрация

урок 1(Страница Вход. Форма, валидачия)

1. устанавливаем хук npm i @hookform/resolver
2. создаём папку pages и компоненнт authorization и работаем с ним
3. импортируем useForm, yup , yupResolver
4. подготавливаем authFormSchema через yuo.object().shape({тут указываем что он должен быть и должен быть строкой, какие символы используем и мин и мак указываем , тоже самое для пароля и также укажем ошибки если условия не соблюдаются})
5. далее делаем компонент Authorization в нём пишем хук useForm который принимает defultValue указываем login и password, а принимаем register, handleSubmit и объект с formState , так же хук принимает resolver: yupResolver(authFormSchema)
6. рисуем нашу разметку h2 - авторизация , делаем форму и передаём ей onSubmit в form будет 2 input и в ней указываем type, placeholder, и функцию {...register('login')} и так же для пароля
7. задаём кнопку для того чтобы войти у неё тип submit и так же описываем фу-ю submit , принимаем login и password , импортируем функцию server из bff
8. обращаемся к server у неё вызываем authorize(login, passsord) через then(({error,res})) делаем запрос, обрабатываем ошибку
9. создаём локальный стейт для ошибки
10. ошибку запроса выводим через set-функцию нашего локального стейта
11. нужно разделить ошибки на те что приходят с сервера serverError и с формы formError
12. в разметке выводим ошибку {errorMessage && <div>{errorMessage}</div>}, так же если у нас ошибка формы блокируем кнопку через disabled
13. нужно стилизовать компонент как мы делаем это обычно передаём className в div на верхнем уровне и принимаем его в пропсах classname помечам компонент container и далее задём для него стили
14. там где маршрутизация мы импортируем наш компонент в Route и также индексируем наш компонент
15. через devtools devtools задаём стили приблизительно и display: flex;flex-direction: column;align-items: center;
    & > form {
    display: flex;
    flex-direction: column;
    }копируем их и вставляем , нужно обратить внимае как можно обращаться к полям формы

---

урок 2

1. выносим отдельно компонент Input стилизуем, индексируем его в компоненте передаём className и другие пропсы,ширину(указываем её в пропсах потому что ширина инпуты буду разных сторон), описание стилей смотрим в Input
2. используем его в нашем компоненте авторизация(импортируем его,всё просто заменяем все импуты на этот компонент),width в стилях передаём вот так width: ${({ width = '100%' }) => width};в нашем компоненте авторизации ширину передаём один раз в компонент authorization в form чтобы все инпуты были растянуты по ширине, так как инпуты это дочерние элементы формы
3. выносим кнопку в компонент Button всё тоже самое как и с Input , только задаём другие стили передаём в неё сверху prop children для того чтобы давать название кнопкам, также меняем в нашем authorization меняем теги на компонент
4. в control-panel меняем нашу ссылку styledButton с cursor:pointer на компонент Button и styledButton меняем на styledIcon внутри у нас &:hover {cursor: pointer;}, оставляем его для наших иконок , она у нас одна для кнопки назад, у 2х других кнопок это делать не надо потому что они явдяются прямой ссылкой
5. релизовывваем ссылку регистрация(импоритруем Link) стилизуем её в самом компоненте, в самой форме рализуем её в самом конце перед кнопкой автоизации и ссылкой регистации ошибку формы
6. стилизуем ErrorMessame там где появляется ошибка в самом компоненте authorization(стилизуем обычный div)
7. так как у нас функция register передаётся в компонент Input нам нужен хук forvardRef мы в него оборачиваем InputCintainer в этом компоненте мы передаём ref вторым аргументом и принимаем как обычный проп ref={ref}
8. чтобы сбрасывать нашу ошибку когда мы печатаем в инпутах в функции register передаём onChange: () => setServerError(null), делаем тоже самое для password
9. в useState в аргументы передаём null(забыли изначально передать)
10. создаём общий компонент для H2 стилизуем его так же как Input и Button

---

урок 3

1.  создаём стор как обычно со store(createStore,milldeware) в файле store.js вс как обычно и так же с помощью combineReduser все reducer собираем все вместе
2.  создаём всё для редьюсеров для начала с заглушками там должно быть начальное состояние и экшн и с помощью switch case обрабатываем , не забываем сделать индексацию(index.js)
3.  оборачиваем весь проект в главном компоненте в Provider и передаём в него store
4.  далее обращаемся в компоненте authorization res(ключ который мы будем гонять)для связи между front и back , сам ключ будем хранить в redux store
5.  далее работаем с user-reduser задаём initialState с данными которые мы задавали в уроке 4, пока что мы при SET_SESSION возвращаем ...state,session:action.payload
6.  создаём папку actions
7.  создаём файл для констант action-type.js(создаём объект ACTION_TYPE с ключами констант SET_SESSION:'SET_SESSION')делаем индексацию для папки actions
8.  рабоатем с файлом setSession в type передаём нашу константу и в payload передаём session который принимаем в пропсах
9.  в authorization проверяем если ошибка return(дальше не идём),далее делаем dispatch(setSession(res))
10. в компоненте server в месте где server нам возвращает объект с return {error: null,res: {id: user.id,login: user.login,roleId: user.role_id,session: sessions.create(user),},};
11. в корне bff создаём компонент sessions.js в нём создаём объект export const sessions = {
    list: {},будут храниться данные для пользователя
    create(user) {
    const hash = Math.random().toFixed(50);случайные числа(ключи сессии)

        	this.list[hash] = user;добавляем в list

        	return user;запоминаем
        },
        remove(hash) {
        	delete this.list[hash];если нужно разлогинится
        },

    }; генерация ключа

12. в компоненте server register возвращаем такой же объект как и в authorize
13. в компоненте authorization мы меняем setSession=>setUser(в action делаем тоже самое setUser и в пропсах и payload user)(в action-type(SET_USER))
14. в user-reduser мы возвращаем case ACTION_TYPE.SET_USER: {
    return {
    ...state,
    ...action.payload,
    };
    }

---

урок 4

1. в header делаем условный редеринг roleId === ROLE.GUEST то кнопка войти , если что то другое нужно кнопка выйти и логин
2. roleId получаем через useSelector(отдельно создаём компонент)индексируем это всё
3. таким же делом получаем login как и roleId,экспортируем его
4. дальше делаем кнопку выйти(копируем кнопку назад),ищем новую иконку для этого , в обработчике onClick передаём dispatch
5. создаём новый logout(actionCreator функция),импортируем server в этом компоненте и в нём вызываем server.logout(session), в пропсах принимаем session
6. в компоненте там где кнопка выход в dispatch передаём logout(session)
7. session получаем через selector делаем новый компонент и индексируем его
8. далее обрабатываем новый обработчик в user-reduser case ACTION_TYPE.LOGOUT:return initialUserState;
9. не забываем индексировать наш logout
10. в server в начале компонента мы async logout(session){sessions.remove(session)} - по сути это удаление нашей сессии

---

урок 5

1. первое что исправляем выносим Button и в этом компоненте оставляем только кнопку Войти
2. убираем все лишние отсупы в иконке выйти, для неё сделаем отдельные стили StyledIcon, для всего контеинера RightAligned задаём align-items:center
3. для login делаем отдельно стилизованный компонент const UserName = styled.div`	font-size: 18px;
font-weight: bold;`;
4. в компоненте Icon мы добавляем пропс {...props} для того чтоыб мы могли спользовать обработчик

---

урок 6

1.  далее при выходе сбрасываем форму авторизации
2.  хук useForm так же возвращает функцию reset(мы её вызовем чтобы форма сбросилась)
3.  для этого нам понадобиться хук useStore с методом subsctiption
4.  создаём новый app-reducer(в нём будет хранится данные чтобы сбросить форму)
5.  так как наш logout пройдёт по всем reducers то в нашем новом app-reducer он тоже сработает,intialAppState это будет boolean значение(wasLogout:false)
6.  в проверке когда logout сработал возвращаем return{...state,wasLogout:!state.wasLogout} разварачиваем state и инвертируем его
7.  в компоненте авторизации мы можем сделать за стейтом задача стоит в том чтобы при изменении waslogout запускалась функция reset делаем это в хуке useEffect
8.  так же нам понадобится store сделаем это с помощью хука store = useStore
    useEffect(() => {
    let currentWasLogout = store.getState().app.wasLogout;

        	return store.subscribe(() => {
        		let previousWasLogout = currentWasLogout;
        		currentWasLogout = store.getState().app.wasLogout;

        		if (currentWasLogout !== previousWasLogout) {
        			reset();
        		}
        	});
        }, [reset, store]);

9.  добавляем в store новый редюсер и индексируем его
10. далее делаем редирект на главную, делаем это с помощью условного рендеринга и роутера, в нашем случаее нужно получить roleId мы это делали уже с помощью хука useSelector(selectUserRole)
11. делаем проверку if (roleId !== ROLE.GUEST) {
    return <Navigate to="/" />;
    } то есть если roleId не гость при авторизации мы попадаем на главную

---

урок 7

1. делаем страницу регистрации(register)
2. создаём компонент registration(копируем authorization и чуть подкрючиваем его)
3. с schema дополянем ещё одно поле passcheck(в нем srting('заполните пароль'),required(),oneOf([yup.ref('password'),null],'повтор пароля не совпадает'))oneOf-поля для проверки совпадения пароля введенного в предыдущем инпуте
4. в useForm добавится passcheck в defualtValue
5. меняем поле H2(регистрация) и добавляем новый Input(passcheck) и меняем название кнопки на зарегестрироваться
6. в маршрутизации вместо заглушки импортируем и используем наш новый компонент Register
7. в formError добавляем ещё одно поле || error?.passcheck?.message к уже существующим
8. в обработчике onSubmit меняем server.authrize на server.register
9. в get-user меняем структуру компонента на такую export const getUser = async (loginToFind) =>
   fetch(`http://localhost:3030/users?login=${loginToFind}`)
   .then((loadedUser) => loadedUser.json())
   .then(([loadedUser]) => loadedUser); важно помнить что сервер возвращает Promise и его нужно обрабатывать
10. в компоненте server const existedUser = await getUser(regLogin) проверяем его на уже существующий и если такой уже есть выводим ошибку, далее создаем нового const user = await addUser(regLogin,regPassword) это важно для добавления нового пользователя на сервер и уже из новго user берём данные
11. компонент addUser обрабатываем с помощью json()
12. далее начинаем смотрет что мы можем вынести
13. первое что мы выносим это auth-form-error(импортируем его как обычно и подставляем вместо старого кода)
14. далее создаём кастомный хук для сброса формы ,компонент useResetForm в которую в агрументы мы принимаем функцию reset , так же импортируем useStore и переносим весь useEffect в этот компонент и в наших компонентах вызываем этот кастомный хук с аргументом reset

---

---

---

Страница Пользователи

урок 1(осн верстка)

1. начинаем основную верстку рисовать
2. создаём в pages компонент с папкой users(users.jsx) делаем как обычно котеинер и компонент который будем стилизовать
3. делаем разметку сначала H2(Пользователи) далее в пропсах принимаем className
4. далее див с 3мя элементами(логин, дата регистрации, роль)тоже div и задаём ему className
5. далле через запрос на сервис получим массив с пользователями в разметку используем метод map под 3мя элементами делаем отдельный блок с массивом в котором диструктуризованные данные можно посмотреть в db.json все кроме password
6. внутри массива мы уже делаем 3 элемента которые будут колонками и задаим им разные className
7. в Icon мы переносим cursor:pointer и обёртку удаляем(не связано с темой которую мы делаем)
8. используем dispatch в Icon импортируем его и создаём
9. обязательно используем key={id} так как используем массив
10. те классы которые мы прописали для колонок внтури элементов массива, их же используем для элемнтов мне таблицы
11. данные которые мы получаем с сервера мы выводим в дивы, важно что 3ий див мы меняем на select(выпадающий список) в value передаём roleId а внутри select у нас будет массив из roles в диструктуризации можем поменять имена наших данных({ id: roleId, name: roleName })это объект наеших данных с сервера где мы поменяли имена чтобы они не пересекались
12. во внутрь дива где у нас селект добавляем Icon сохранить
13. в select добаляем option с value={roleId} и выводом {roleName} так же добавляем обработчик onChange
14. делаем отдельный компонент user-row это будет строка с данными переносим все пропсы и импорты удаляем что не нужно, то есть в users мы импортируем <UserRow/>

---

урок 2 довёрстываем страинцу

1.  первым делом компонент Users мы вставляем в нашу маршрутизацию
2.  в компоненте user-row делаем правильные import потому что степень вложенности теперь другая нежели мы скопировали из другого компонента
3.  копируем с компонента Authorization в компонент Users часть стилей display: flex;flex-direction: column;align-items: center; далее тут же добавляем ещё стилей margin: 0 auto; width:570px;
4.  выносим в отдельный компонент и назовём его table-row и внутри его стилизуем внутри сразу, создаём контейнер в котором div и во внутрь передаём children(принимаем в пропсах) этот компонент мы будем переиспользовать задаём стили этому компоненту display: flex;
    align-items: center;

        & > div {
        	padding: 0 10px;
        }
        & .login-column {
        	width: 172px;
        }

        & .registered-column {
        	width: 213px;
        }

        & .role-column {
        	width: auto;
        }

    мы тут задём общие стили и отдельно ширину для каждого элемента в контейнере через className

5.  важный момнт когда мы меняли div на компонент TableRow нужно делать import напрямую а не через index(причина потому что он не является дочерним элементом в структуре файлом , грубо говоря если бы мы импортировали из index.js то мы бы импортировали самого себя так как в индексации есть и наш файл тоже ) всё это может привести к цеклической зависимости и приложение зависнет
6.  важно в компоненте user-row мы используем table-row для стилизации колонок

---

урок 3

1.  будем рабоать с proxy в этом уроке
2.  нужно навести порядок в дереве проекта, заводим папку oprations в которой(авторизация,регистрация,выход и создадим ещё один компонент запрос ролей(fetch-roles))
3.  отдельно создаём папку api(в ней запросы на сервер в получении информации)
4.  для генерации даты создадим папку utils индексируем её и делаем правильный импорт
5.  в корне папки bff оставляем 3 файла помимом папок(server,sessions,index)
6.  в options компонетах у нас будет методы которые нас раньше были в компоненте server, мы его разгружаем чтобы он был более читабельнее
7.  важно чтобы наши компоненты на фронтенде импортировали только bff наш фейк запрос(мы его делаем на фронтенде)
8.  начинаем создавать fetch-roles(в этом компоненте мы проверяем имеет ли пользователь права чтобы получить список ролей)
9.  создаём в api компонент get-roles(для получения ролей) индексируем его
10. в fetch-roles мы должны проверить есть ли у пользователя права на это(получение списка ролей) это должно быть только у администратора, в этом компоненте мы в аргументы принимаем userSession,accessRoles(роль администратор для удобства мы копируем константу в bff которая у на есть чтобы мы работали только с bff(принцып изолированости proxy от frontend)) и отправляем её на проверку доступа
11. в компоненте мы создаём accessRoles=[ROLE.ADMIN] тот кто может получить список пользователей
12. В getUser мы делаем это .then(
    ([loadedUser]) =>
    loadedUser && {
    id: loadedUser.id,
    login: loadedUser.login,
    password: loadedUser.password,
    registeredAt: loadedUser.registeredAt,
    roleId: loadedUser.roleId,
    },) получаем сразу данные а не объект с ними
13. так как наш getUser исменился мы дорабаываем наш компонент authrize с диструктуризированными данными
14. в компоннете sessions добавляем метод access(hash, accesRoles) {
    const user = this.list[hash];

        return !!user && accesRoles.includes(user.roleId);

    },проверяем есть ли сессия и является ли наш пользователь администаратором

15. в компоненте fetch-roles выдаём ошибку если роли администратора нет, если есть возвращаем данные

---

урок 4

1. начинаем реализовывать кастомный хук useServerRequest импортируем(тут server из bff) получаем тут session(сессия из менеджера сосотояния)
2. в хуке мы формируем данные для запроса и эти данные будут либо включать session либо нет с помощью тенарного оператора
   const request = ['register', 'authorize'].includes(operation)
   ? params
   : [session, ...params]
   возвращает она return server[operation](...request) не забываем его импортировать, наш request мы помещаем в callback функцию, в аргументах у нас opiration и ...params
3. импортруем этот хук в Users и сразу используем const requestServer = useServerRequest();
4. создаём 3 состояние users,roles,errorMassage(в начальном состоянии пустой массив кроме errorMassage там будет null) в компонент userRow отправяем в качестве пропсов roles там они понадобятся
5. с помощью условного рендеринга мы выводим ошибку или разметку(если роль не та что нужна то мы выводим ошибку если всё в порядке выводим контент)
6. нам нужны так же users,roles для этого в useEffect мы используем Promise.all в ответ мы получаем usersRes и rolesRes и начинаем обрабатывать эти данные а конкретно сначала ошибки if (usersRes.error || rolesRes) {
   setErrorMassage(usersRes.error || rolesRes);
   return; важно писать return в этом случае тормозим функцию,если ошибок нету setUsers(usersRes);setRoles(rolesRes);
   }
7. вернёмся к тому чтобы вынести кусок кода с условным рендерингом и назовём его Content(для этого переименуюм нашу обёртку над роутингом переименуем его в Page) в компоненте Content в пропсах принимаем ({children}) и используем услоный рендеринг если ошибка выводим ошибку с заголовком ошибка еслли ошибки нет выводим children(импорт H2 происходит напрямную из компонента где он находится чтобы небыло циклической зависимости) для контента с ошибкой условного рендеринга мы стилизуем компонент в котором ошибка и заголовок , стилизуем его display: flex;flex-direction: column;align-items: center; индексируем этот компонент
8. используем наш новый компонент в разметке Users в пропсы <Content error={errorMessage}>
9. в массив зависимостей useEffect мы передаём requestServer(чтобы он не перерэндаривался мы в нашем кастомном хуке нашу функцию заворачиваем в useCallback в массив зависимостей которого передаём session)
