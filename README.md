Начало Работ

1 урок
установили пакеты которые нам нужны(redux,react-redux,redux-thunk,react-hook-form,styled-components,react-router,yup,prop-types и json-server global)
преобразовали index.html
подчистили все файлы удалили лишние css файлы так как будем рабоать с styled-components и дали первый стиль для первого блочного элемента Div

---

урок 2
скачали иконочный шрифт,закинули в проект
в папке index.css импортировали то что мы закинули в проект,важно что в самом файле font-awesome.css изменили путь к файлам так как изменили название папки
познакомились с сайтом fontawesome потренировались с иконками(то зачем мы всё скачали)
сделали и закометили ветку с помощь git(BLOG-003)

---

урок 3
области хранения данных:
-база данных на json-server
-BFF
-редакс стор

сущности и приложение:
-пользователь : БД(список пользователей),BFF(ссесия текущего),стор(отображение в браузере)
-роль пользователя: БД(список ролей),BFF(сессия пользователя),стор(использование на клиенте)
-статья:БД(список статей),стор(отображение в браузере)
-коментарии:БД(список комментариев),стор(отображение в браузере)

таблицы БД:

- пользователи - users: id / login / password / registed_at / role_id
- роли - roles: id / name
  -статьи - posts: id / title / image_url / content / published_at
  -комметарии - comments: id / author_id / post_id / content

---

урок 4
Схема состояния на BFF:
-сессия текущего пользователя: login / password / role

Схема для редакс стора (на клиенте):
-user: id / login / roleId / session
-posts: массив post: id / title / imageUrl / content / publishedAt
-post: id / title /imageUrl / content / publishedAt / comments: массив comment: id / author / content / publishedAt
-users: массив user: id / login / registeredAt / role

---

урок 5
посмотрели наброски в photopea и зашли на сайт рыбаТекст познакомились с приложением генератор текста , прикинули как будет выглядить сайт

---

урок 6
далее начинаем заполнять нашу базу данных которую мы создали в папке src db.json и начинаем прописывать там информацию , сделали и заполнили db.json (сделали 4 массива(users,roles,posts,comments) и запустили server ответы он присылает на хоте 3005)

---

урок 7
начали первично создавать BFF(бэкенд для фронтенда) реализуем его локально , создали папку bff и файл bff.js внутри создаём метод authorize который принимает логин и пароль с помощью fetch запроса с ссылкой на базу данных шашей db.json , создали переменую которая нам вернём массив users, далее с помощью метода find ищем совпадение логинов. делаем проверку с помощью if , если ответ не получен то выводим ошибку error с текстом и res:null . Далее проверяем чтобы пароли совподали с помощью if если наш authPassword !== users.password возвращаем error и res:null , если логин и пароль совпали возвращаем error:null и res(например removeComment logout(разлогинится) для этого мы берём ключи Object.keys(session) проходимся с помощью forEach((key) => { delete session[key]}) удаляем все ключи).

---

урокм 8
далее реализовываем первичную реализацию bff(регистрация) , но только уже для пользователя который регистрируется , так же делаем запрос на сервер , он возвращает ответ и тут мы уже проверяем еслит наш user есть , то возвращаем ошибку , двигаемся дальше с помощью method:'POST' добавляем полтзователя со всеми данными на сервер с ролью читатель и для даты регистрации мы добавили случайную генерацию числе , далее так же подключаем session и уже возвращаем error:null и res:session

---

урок 9
дальше создаём первичную реализацию bff(рефакторинг.сессии)наводим порядок в коде

1. создали get-users(запрос пользователей от базы данных)
2. generate-date переместили в отдельный файл и вызываем там где это необходимо
3. далее создаём get-user в этот файл мы импортируем get-users в этом компанете мы вызываем async,aweit так как мы получааем много операций и далее мы возвращаем одного пользователя
4. дальше разгружаем наш основной компонент и уже там вызываем функцию getUser с перевым аргументом который будет authLogin для авторизадции и regLogin для регистрации
5. создаём add-user в этот файл импортируем generateDate и в аргументы нашей функции передаём login и password (в body мы передаём login и password как обычные значения)
6. в нашем основном компоненте вызываем функцию add-user и передаём в неё regLogin и regPassword
7. создаём create-session в аргументы принимаем roleId , создаём объект session и передаём в него logout ,далее с помощью switch/case по roleId мы раздаём права какие могут быть
8. создаём папку session и в ней созд компонент removeComment и дальше его импортируем в папке create-session
9. далее выносим roleId в константы для этого создаём папку constants и в ней файл role с пояснением какая цифра какой роли ровняется,гостя не указываем и так же индексируем
10. далее импортруем наш объект ROLE и в switch/case указываем роли и заготавливаем для 3х ролей права для удаления админ может, модератор тоже , читатель нет
11. в нашем bff в res передаём createSession(user.role_id) как указано на нашем сервере
12. закомитиил наши изменения(создали bff)

---

укро 10

1. начали реализовывать основной компонент блога(переименуем его в Blog) и сам компонент переименовываем и так же в импорте тоже
2. зададим пустые Herader и Footer они будут неизменные
3. далее будем работать с routing , импортируем BrowserRouter оборачиваем наш Blog
4. импортируем Routes и Route вставляем в проект и у нас будет 8 страниц (главная,авторизация,регистрация,пользователи,статья,новая статья,ошибка(у нас их две будет))
5. в styled content передаём padding 120px
6. для styled h2 задаём центровку

---

урок 11

1. начинаем рализацию шапку странцы
2. в основном css файле задаём цвет фона #405060
3. для html body и #root задали высоту 100%
4. возьмём наш основной компонент и стелизуем его в компонент AppColumn
5. начинаем работать с шапкой т.к. у нас целый компонент синтаксис стилизации чуть отличаеться styled(Header)` и далее прописываем стили и используем его уже в разметке , так же в наш компонент в пропсы передаём объект с className и в разметке передаём className привычным способом проверяем это всё дело в DevTools
6. ищем в fontawesome нашу иконку котрую мы уже продумали до этого и копируем её </> наш логотип
7. создаём папку components также index.js
8. так же создаём папку header в самом компоненте уже стизизуем наш компонент header(проверяем в браузере чтобы всё работало)
9. в папке header создаём папку components и добавляем папку logo и создаём файл logo.jsx так же стилизуем этот компонент в этом компоненте добавляем так же 1.Блог 2.веб-разработчика и так же их стилизуем
10. подключаем наш Logo в файле header(и проверяем в браузере)
11. стилизуем наши компоненты в logo.jsx можно это делать черрез инструменты разработчика задаём все отступы и добавляем размеры
12. в самом header задаём отступы padding
13. в общем css применяем ко всем тегам box-sizing: border-box
14. изза того что наш логотип съехал в компоненте logo делаем отризацельный margin-top: -14px
15. далее делаем тень для нашего header и фиксируем его к верху через position:fixed и top:0 и также зададим ширину которую мы до этого обозначили 1000px и для того чтобы крыть контент который должен уезжать под шапку , делаем белый фон

---

урок 12

1. дальше делаем вторую колонку в шапе Discription применяем стили к Header display:flex и justify-content:space-between чтобы растянуть контент вдоль всего header
2. применяем font-stile:italic
3. делаем компонент для кнопок ControlPanel
4. создаём ControlPanelContainer в пропсы принимаем объект с className и разметка
5. далее создаём компонент RightAligned(внутри которого уже будут кнопки) и добаляем в него стили и добавляем его в наш ControlPanelContainer
6. не забываем его экспортировать через index.js
7. вставляем наш элемент ControlPanel после Discription
8. Делаем обший копонент icon.jsx для того чтобы его переиспользовать
9. в пропсы принимае id и выводим запись уже в компоненте также в styledComponent через функцию в которой мы принимаем пропсы выводим размер и отступы и изначально зададим им значения size = '24px' margin = '0'
10. подключаем в лого на новый компоненнт Icon принимаем в пропсы(fa-code font-size(70px) и margin(0 10px 0 0))
11. в ControlPAnel подключаем Icon 3 наших иконки по примеру с logo
12. увеличиваем кнопку войти через стилизацию

---

урок 13

1. реализовываем функционал шапки , то есть всех кнопок
2. импортируем Link из react-router-dom
3. в LogoContainer заменяем div на Link и to="/" переход на главную страницу
4. в основном компоненте css для ссылок прописываем color:#000 и text-decoration: none , чтобы текст был черным и не было подчёркиваний
5. в компонент ConrtolPanel так импоритируем Link и кнопку button мы меняем на ссылку делаем ей border: 1px solid #000(рамку), центруем текст в рамке с помощью display:flex, justify-content:center, align-items:center(но можно было обернуть саму кнопку в Link) и добавляем цвет на фон в to="/login"
6. для кнопок новая статью и пользователи всё достаточно просто оборачиваем их в Link и прописываем в to="/post" для новая статьтся и to="/users" для пользователей
7. для кнопки назад импортируем хук useNavigate, на верхнем уровне компонента мы создаём переменную navigate , так как onClick нужно передавать в обёртку Icon то есть в родительском компоненте мы нашу обёртку это будет div назавём её StyledButton и стилизуем его &hover{ cursor:pointer;} в onClick={()=>navigate(-1)} передаём логику назад
8. и поднимем чуть чучть наш логотип margin-top:-21px(это легче делать в инструментах разработчика)

---

урок 14

1. начинаем реализовывать футер
2. начнём с самого сложного реализуем виджет погоды(можем это сделать с помощью яндекса с помощью api-ключа на сайте API Яндекс.Погода)
3. с помощью fetch пытаемся отправить запрос из консоли
4. fetch(url,headers:{'Content-Type':'aplication/json',X-Yandex-API-Key:'какой-то ключ'}).then((data)=>data.json()) и всё это выводим в консоль
5. по CORS(сервис против хакеров) политике запрос не пришёл так как яндекс не даёт с локального сервера и яндекс блокирует запрос , альтернативный выход размещать этот запрос на сервере , не наш вариант так как у нас сервер на фронтенде
6. мы будем использовать Open Weather
7. регестрируемся, создаём ключ , и в документации смотрим какую ручку(адрес запроса) мы будем создавать
8. проверяем это всё через консоль прямов браузере

---

урок 15

1. создаём компонент Footer асинхронные запросы делаем в специальном хуке useEffect
2. в хуке используем наш запрос fetch(и обрабатываем их с помощью json и принимаем объект с данными(name,main,weather) для вывода данных)
3. создаём 3 состояния(city,temperature,weather) и в хуке useEffect в then(через set-функции устанавливаем наши значения полученные с ручки)
4. не забываем эспортировать в index.js наш footer
5. начинаем верстать(всё оборачиваем в обёртки слева мы пишем блог веб-разработчика и почту указываем)
6. справа мы выводим температуру, описание погоды и название города , так же в нашей родительской обёртке div на верхнем уровне footer добавяем className={className} а принимаем его из аргументов FooterContainer для того чтобы мы могли его стилизовать
7. дальше стилизуем наш компонент с помощью styledComponet
8. задаём display:felx,justify-content:space-between,align-items:center для того чтобы контент в нашем footer был распределён по разным сторонам слева и справа
9. так в инструментах разработчика мы смотрим как мы делали шапку(высоту,ширину,падинги,цвет фона и тень от рамки,так же добавим жирный текст(width: 1000px;height: 120px;padding: 20px 40px;font-weight: bold;background-color: #fff;box-shadow: 0 2px 17px #000;)в шапке мы смещали на -2px а в footer мы её смещаем на 2px чтобы тень выглядела одинаково)
10. так же справа выводим текущаю дату, там где мы указывали город {new Date().toLocaleString('ru', { day: 'numeric', month: 'long' })}

---

---

---

Вход и регистрация

урок 1(Страница Вход. Форма, валидачия)

1. устанавливаем хук npm i @hookform/resolver
2. создаём папку pages и компоненнт authorization и работаем с ним
3. импортируем useForm, yup , yupResolver
4. подготавливаем authFormSchema через yuo.object().shape({тут указываем что он должен быть и должен быть строкой, какие символы используем и мин и мак указываем , тоже самое для пароля и также укажем ошибки если условия не соблюдаются})
5. далее делаем компонент Authorization в нём пишем хук useForm который принимает defultValue указываем login и password, а принимаем register, handleSubmit и объект с formState , так же хук принимает resolver: yupResolver(authFormSchema)
6. рисуем нашу разметку h2 - авторизация , делаем форму и передаём ей onSubmit в form будет 2 input и в ней указываем type, placeholder, и функцию {...register('login')} и так же для пароля
7. задаём кнопку для того чтобы войти у неё тип submit и так же описываем фу-ю submit , принимаем login и password , импортируем функцию server из bff
8. обращаемся к server у неё вызываем authorize(login, passsord) через then(({error,res})) делаем запрос, обрабатываем ошибку
9. создаём локальный стейт для ошибки
10. ошибку запроса выводим через set-функцию нашего локального стейта
11. нужно разделить ошибки на те что приходят с сервера serverError и с формы formError
12. в разметке выводим ошибку {errorMessage && <div>{errorMessage}</div>}, так же если у нас ошибка формы блокируем кнопку через disabled
13. нужно стилизовать компонент как мы делаем это обычно передаём className в div на верхнем уровне и принимаем его в пропсах classname помечам компонент container и далее задём для него стили
14. там где маршрутизация мы импортируем наш компонент в Route и также индексируем наш компонент
15. через devtools devtools задаём стили приблизительно и display: flex;flex-direction: column;align-items: center;
    & > form {
    display: flex;
    flex-direction: column;
    }копируем их и вставляем , нужно обратить внимае как можно обращаться к полям формы

---

урок 2

1. выносим отдельно компонент Input стилизуем, индексируем его в компоненте передаём className и другие пропсы,ширину(указываем её в пропсах потому что ширина инпуты буду разных сторон), описание стилей смотрим в Input
2. используем его в нашем компоненте авторизация(импортируем его,всё просто заменяем все импуты на этот компонент),width в стилях передаём вот так width: ${({ width = '100%' }) => width};в нашем компоненте авторизации ширину передаём один раз в компонент authorization в form чтобы все инпуты были растянуты по ширине, так как инпуты это дочерние элементы формы
3. выносим кнопку в компонент Button всё тоже самое как и с Input , только задаём другие стили передаём в неё сверху prop children для того чтобы давать название кнопкам, также меняем в нашем authorization меняем теги на компонент
4. в control-panel меняем нашу ссылку styledButton с cursor:pointer на компонент Button и styledButton меняем на styledIcon внутри у нас &:hover {cursor: pointer;}, оставляем его для наших иконок , она у нас одна для кнопки назад, у 2х других кнопок это делать не надо потому что они явдяются прямой ссылкой
5. релизовывваем ссылку регистрация(импоритруем Link) стилизуем её в самом компоненте, в самой форме рализуем её в самом конце перед кнопкой автоизации и ссылкой регистации ошибку формы
6. стилизуем ErrorMessame там где появляется ошибка в самом компоненте authorization(стилизуем обычный div)
7. так как у нас функция register передаётся в компонент Input нам нужен хук forvardRef мы в него оборачиваем InputCintainer в этом компоненте мы передаём ref вторым аргументом и принимаем как обычный проп ref={ref}
8. чтобы сбрасывать нашу ошибку когда мы печатаем в инпутах в функции register передаём onChange: () => setServerError(null), делаем тоже самое для password
9. в useState в аргументы передаём null(забыли изначально передать)
10. создаём общий компонент для H2 стилизуем его так же как Input и Button

---

урок 3

1.  создаём стор как обычно со store(createStore,milldeware) в файле store.js вс как обычно и так же с помощью combineReduser все reducer собираем все вместе
2.  создаём всё для редьюсеров для начала с заглушками там должно быть начальное состояние и экшн и с помощью switch case обрабатываем , не забываем сделать индексацию(index.js)
3.  оборачиваем весь проект в главном компоненте в Provider и передаём в него store
4.  далее обращаемся в компоненте authorization res(ключ который мы будем гонять)для связи между front и back , сам ключ будем хранить в redux store
5.  далее работаем с user-reduser задаём initialState с данными которые мы задавали в уроке 4, пока что мы при SET_SESSION возвращаем ...state,session:action.payload
6.  создаём папку actions
7.  создаём файл для констант action-type.js(создаём объект ACTION_TYPE с ключами констант SET_SESSION:'SET_SESSION')делаем индексацию для папки actions
8.  рабоатем с файлом setSession в type передаём нашу константу и в payload передаём session который принимаем в пропсах
9.  в authorization проверяем если ошибка return(дальше не идём),далее делаем dispatch(setSession(res))
10. в компоненте server в месте где server нам возвращает объект с return {error: null,res: {id: user.id,login: user.login,roleId: user.role_id,session: sessions.create(user),},};
11. в корне bff создаём компонент sessions.js в нём создаём объект export const sessions = {
    list: {},будут храниться данные для пользователя
    create(user) {
    const hash = Math.random().toFixed(50);случайные числа(ключи сессии)

        	this.list[hash] = user;добавляем в list

        	return user;запоминаем
        },
        remove(hash) {
        	delete this.list[hash];если нужно разлогинится
        },

    }; генерация ключа

12. в компоненте server register возвращаем такой же объект как и в authorize
13. в компоненте authorization мы меняем setSession=>setUser(в action делаем тоже самое setUser и в пропсах и payload user)(в action-type(SET_USER))
14. в user-reduser мы возвращаем case ACTION_TYPE.SET_USER: {
    return {
    ...state,
    ...action.payload,
    };
    }

---

урок 4

1. в header делаем условный редеринг roleId === ROLE.GUEST то кнопка войти , если что то другое нужно кнопка выйти и логин
2. roleId получаем через useSelector(отдельно создаём компонент)индексируем это всё
3. таким же делом получаем login как и roleId,экспортируем его
4. дальше делаем кнопку выйти(копируем кнопку назад),ищем новую иконку для этого , в обработчике onClick передаём dispatch
5. создаём новый logout(actionCreator функция),импортируем server в этом компоненте и в нём вызываем server.logout(session), в пропсах принимаем session
6. в компоненте там где кнопка выход в dispatch передаём logout(session)
7. session получаем через selector делаем новый компонент и индексируем его
8. далее обрабатываем новый обработчик в user-reduser case ACTION_TYPE.LOGOUT:return initialUserState;
9. не забываем индексировать наш logout
10. в server в начале компонента мы async logout(session){sessions.remove(session)} - по сути это удаление нашей сессии

---

урок 5

1. первое что исправляем выносим Button и в этом компоненте оставляем только кнопку Войти
2. убираем все лишние отсупы в иконке выйти, для неё сделаем отдельные стили StyledIcon, для всего контеинера RightAligned задаём align-items:center
3. для login делаем отдельно стилизованный компонент const UserName = styled.div`	font-size: 18px;
font-weight: bold;`;
4. в компоненте Icon мы добавляем пропс {...props} для того чтоыб мы могли спользовать обработчик

---

урок 6

1.  далее при выходе сбрасываем форму авторизации
2.  хук useForm так же возвращает функцию reset(мы её вызовем чтобы форма сбросилась)
3.  для этого нам понадобиться хук useStore с методом subsctiption
4.  создаём новый app-reducer(в нём будет хранится данные чтобы сбросить форму)
5.  так как наш logout пройдёт по всем reducers то в нашем новом app-reducer он тоже сработает,intialAppState это будет boolean значение(wasLogout:false)
6.  в проверке когда logout сработал возвращаем return{...state,wasLogout:!state.wasLogout} разварачиваем state и инвертируем его
7.  в компоненте авторизации мы можем сделать за стейтом задача стоит в том чтобы при изменении waslogout запускалась функция reset делаем это в хуке useEffect
8.  так же нам понадобится store сделаем это с помощью хука store = useStore
    useEffect(() => {
    let currentWasLogout = store.getState().app.wasLogout;

        	return store.subscribe(() => {
        		let previousWasLogout = currentWasLogout;
        		currentWasLogout = store.getState().app.wasLogout;

        		if (currentWasLogout !== previousWasLogout) {
        			reset();
        		}
        	});
        }, [reset, store]);

9.  добавляем в store новый редюсер и индексируем его
10. далее делаем редирект на главную, делаем это с помощью условного рендеринга и роутера, в нашем случаее нужно получить roleId мы это делали уже с помощью хука useSelector(selectUserRole)
11. делаем проверку if (roleId !== ROLE.GUEST) {
    return <Navigate to="/" />;
    } то есть если roleId не гость при авторизации мы попадаем на главную

---

урок 7

1. делаем страницу регистрации(register)
2. создаём компонент registration(копируем authorization и чуть подкрючиваем его)
3. с schema дополянем ещё одно поле passcheck(в нем srting('заполните пароль'),required(),oneOf([yup.ref('password'),null],'повтор пароля не совпадает'))oneOf-поля для проверки совпадения пароля введенного в предыдущем инпуте
4. в useForm добавится passcheck в defualtValue
5. меняем поле H2(регистрация) и добавляем новый Input(passcheck) и меняем название кнопки на зарегестрироваться
6. в маршрутизации вместо заглушки импортируем и используем наш новый компонент Register
7. в formError добавляем ещё одно поле || error?.passcheck?.message к уже существующим
8. в обработчике onSubmit меняем server.authrize на server.register
9. в get-user меняем структуру компонента на такую export const getUser = async (loginToFind) =>
   fetch(`http://localhost:3030/users?login=${loginToFind}`)
   .then((loadedUser) => loadedUser.json())
   .then(([loadedUser]) => loadedUser); важно помнить что сервер возвращает Promise и его нужно обрабатывать
10. в компоненте server const existedUser = await getUser(regLogin) проверяем его на уже существующий и если такой уже есть выводим ошибку, далее создаем нового const user = await addUser(regLogin,regPassword) это важно для добавления нового пользователя на сервер и уже из новго user берём данные
11. компонент addUser обрабатываем с помощью json()
12. далее начинаем смотрет что мы можем вынести
13. первое что мы выносим это auth-form-error(импортируем его как обычно и подставляем вместо старого кода)
14. далее создаём кастомный хук для сброса формы ,компонент useResetForm в которую в агрументы мы принимаем функцию reset , так же импортируем useStore и переносим весь useEffect в этот компонент и в наших компонентах вызываем этот кастомный хук с аргументом reset

---

---

---

Страница Пользователи

урок 1(осн верстка)

1. начинаем основную верстку рисовать
2. создаём в pages компонент с папкой users(users.jsx) делаем как обычно котеинер и компонент который будем стилизовать
3. делаем разметку сначала H2(Пользователи) далее в пропсах принимаем className
4. далее див с 3мя элементами(логин, дата регистрации, роль)тоже div и задаём ему className
5. далле через запрос на сервис получим массив с пользователями в разметку используем метод map под 3мя элементами делаем отдельный блок с массивом в котором диструктуризованные данные можно посмотреть в db.json все кроме password
6. внутри массива мы уже делаем 3 элемента которые будут колонками и задаим им разные className
7. в Icon мы переносим cursor:pointer и обёртку удаляем(не связано с темой которую мы делаем)
8. используем dispatch в Icon импортируем его и создаём
9. обязательно используем key={id} так как используем массив
10. те классы которые мы прописали для колонок внтури элементов массива, их же используем для элемнтов мне таблицы
11. данные которые мы получаем с сервера мы выводим в дивы, важно что 3ий див мы меняем на select(выпадающий список) в value передаём roleId а внутри select у нас будет массив из roles в диструктуризации можем поменять имена наших данных({ id: roleId, name: roleName })это объект наеших данных с сервера где мы поменяли имена чтобы они не пересекались
12. во внутрь дива где у нас селект добавляем Icon сохранить
13. в select добаляем option с value={roleId} и выводом {roleName} так же добавляем обработчик onChange
14. делаем отдельный компонент user-row это будет строка с данными переносим все пропсы и импорты удаляем что не нужно, то есть в users мы импортируем <UserRow/>

---

урок 2 довёрстываем страинцу

1.  первым делом компонент Users мы вставляем в нашу маршрутизацию
2.  в компоненте user-row делаем правильные import потому что степень вложенности теперь другая нежели мы скопировали из другого компонента
3.  копируем с компонента Authorization в компонент Users часть стилей display: flex;flex-direction: column;align-items: center; далее тут же добавляем ещё стилей margin: 0 auto; width:570px;
4.  выносим в отдельный компонент и назовём его table-row и внутри его стилизуем внутри сразу, создаём контейнер в котором div и во внутрь передаём children(принимаем в пропсах) этот компонент мы будем переиспользовать задаём стили этому компоненту display: flex;
    align-items: center;

        & > div {
        	padding: 0 10px;
        }
        & .login-column {
        	width: 172px;
        }

        & .registered-column {
        	width: 213px;
        }

        & .role-column {
        	width: auto;
        }

    мы тут задём общие стили и отдельно ширину для каждого элемента в контейнере через className

5.  важный момнт когда мы меняли div на компонент TableRow нужно делать import напрямую а не через index(причина потому что он не является дочерним элементом в структуре файлом , грубо говоря если бы мы импортировали из index.js то мы бы импортировали самого себя так как в индексации есть и наш файл тоже ) всё это может привести к цеклической зависимости и приложение зависнет
6.  важно в компоненте user-row мы используем table-row для стилизации колонок

---

урок 3

1.  будем рабоать с proxy в этом уроке
2.  нужно навести порядок в дереве проекта, заводим папку oprations в которой(авторизация,регистрация,выход и создадим ещё один компонент запрос ролей(fetch-roles))
3.  отдельно создаём папку api(в ней запросы на сервер в получении информации)
4.  для генерации даты создадим папку utils индексируем её и делаем правильный импорт
5.  в корне папки bff оставляем 3 файла помимом папок(server,sessions,index)
6.  в options компонетах у нас будет методы которые нас раньше были в компоненте server, мы его разгружаем чтобы он был более читабельнее
7.  важно чтобы наши компоненты на фронтенде импортировали только bff наш фейк запрос(мы его делаем на фронтенде)
8.  начинаем создавать fetch-roles(в этом компоненте мы проверяем имеет ли пользователь права чтобы получить список ролей)
9.  создаём в api компонент get-roles(для получения ролей) индексируем его
10. в fetch-roles мы должны проверить есть ли у пользователя права на это(получение списка ролей) это должно быть только у администратора, в этом компоненте мы в аргументы принимаем userSession,accessRoles(роль администратор для удобства мы копируем константу в bff которая у на есть чтобы мы работали только с bff(принцып изолированости proxy от frontend)) и отправляем её на проверку доступа
11. в компоненте мы создаём accessRoles=[ROLE.ADMIN] тот кто может получить список пользователей
12. В getUser мы делаем это .then(
    ([loadedUser]) =>
    loadedUser && {
    id: loadedUser.id,
    login: loadedUser.login,
    password: loadedUser.password,
    registeredAt: loadedUser.registeredAt,
    roleId: loadedUser.roleId,
    },) получаем сразу данные а не объект с ними
13. так как наш getUser исменился мы дорабаываем наш компонент authrize с диструктуризированными данными
14. в компоннете sessions добавляем метод access(hash, accesRoles) {
    const user = this.list[hash];

        return !!user && accesRoles.includes(user.roleId);

    },проверяем есть ли сессия и является ли наш пользователь администаратором

15. в компоненте fetch-roles выдаём ошибку если роли администратора нет, если есть возвращаем данные

---

урок 4

1. начинаем реализовывать кастомный хук useServerRequest импортируем(тут server из bff) получаем тут session(сессия из менеджера сосотояния)
2. в хуке мы формируем данные для запроса и эти данные будут либо включать session либо нет с помощью тенарного оператора
   const request = ['register', 'authorize'].includes(operation)
   ? params
   : [session, ...params]
   возвращает она return server[operation](...request) не забываем его импортировать, наш request мы помещаем в callback функцию, в аргументах у нас opiration и ...params
3. импортруем этот хук в Users и сразу используем const requestServer = useServerRequest();
4. создаём 3 состояние users,roles,errorMassage(в начальном состоянии пустой массив кроме errorMassage там будет null) в компонент userRow отправяем в качестве пропсов roles там они понадобятся
5. с помощью условного рендеринга мы выводим ошибку или разметку(если роль не та что нужна то мы выводим ошибку если всё в порядке выводим контент)
6. нам нужны так же users,roles для этого в useEffect мы используем Promise.all в ответ мы получаем usersRes и rolesRes и начинаем обрабатывать эти данные а конкретно сначала ошибки if (usersRes.error || rolesRes) {
   setErrorMassage(usersRes.error || rolesRes);
   return; важно писать return в этом случае тормозим функцию,если ошибок нету setUsers(usersRes);setRoles(rolesRes);
   }
7. вернёмся к тому чтобы вынести кусок кода с условным рендерингом и назовём его Content(для этого переименуюм нашу обёртку над роутингом переименуем его в Page) в компоненте Content в пропсах принимаем ({children}) и используем услоный рендеринг если ошибка выводим ошибку с заголовком ошибка еслли ошибки нет выводим children(импорт H2 происходит напрямную из компонента где он находится чтобы небыло циклической зависимости) для контента с ошибкой условного рендеринга мы стилизуем компонент в котором ошибка и заголовок , стилизуем его display: flex;flex-direction: column;align-items: center; индексируем этот компонент
8. используем наш новый компонент в разметке Users в пропсы <Content error={errorMessage}>
9. в массив зависимостей useEffect мы передаём requestServer(чтобы он не перерэндаривался мы в нашем кастомном хуке нашу функцию заворачиваем в useCallback в массив зависимостей которого передаём session)

---

урок 5

1. в этом уроке мы отлавливаем ошибки
2. в server мы добавляем fetchUsers и fetchRoles индексируем их и добавляем fetch-users будет похож на fetch-roles(меняем имена,понадобится getUers)
3. основная ошибка в том что в useEffect мы setаем данные не так, правильно(setRoles(rolesRes.res),setUsers(usersRes.res))
4. далее в массиве ролей обязательно нуден в <UserRow> в option мы передаём key={roleId}
5. далее делаем трансформер преобразовываем в другой формат данных(в пропсах принимаем dbUser-одного пользователя и извлекаем его данные) индексируем его
6. так же в getUsers мы тоже используем трансформер, но чуть чуть по другму .then((loadedUsers) => loadedUsers && loadedUsers.map(transformUser))

---

урок 6

1. первым делом мы поправим нашу верстку
2. в компоненте users дополнительно зададим размер текста font-size 18px;
3. для tableRow мы дополнительно в div задаём display: flex;(чтобы все элементы были в строку) для userRow применяем тоже самое display: flex + margin-top: 10px;
4. для tableRow передаём проп border={true},а в самом компоненте где мы описываем стили border: ${({ border }) => (border ? '1px solid #000' : 'none')}; и из этого получается что если есть бордер стили применятся
5. в select применяем пару стилей & select
   {paddind: 0 5px;font-size: 16px;}
6. начинаем реализовывать переключение onRoleChange(для начала создадим состояние useState и начальным значение передадим userRoleId) который принимает target и возвращает setSelectedRoleId(target.value) и в значение value мы передаём selectedRoleId
7. реализовывваем иконку сохранить для этого мы будем менять цвет иконки в зависимости от того какое value выбрано в первую очередь в самой Icon мы передаём следующее color: ${({ disabled }) => (disabled ? '#ccc' : '#000')}; в пропсы в user-row мы передаём пропс disabled={isSaveButtonDisabled}
8. isSaveButtonDisabled из себя представляет boolean значение при котором selectedRoleId(то что в нашем селекторе выбрано) === userRoleID(то что нам приходит в пропсах) ОЧЕНЬ ВАЖНЫЙ МОМЕНТ КОГДА МЫ SETАЕМ В ОБРАБОТЧИКЕ onRoleChange нам приходит строка её нужно преобразовать в число, если disabled = true иконка бледная если наоборот светлая

---

урок 7

1. реализовываем кнопку сохранение смены роли
2. первымм делом создаём api запрос set-user-role в котором используем метод PATCH с помощью него мы меняем только один элемент role_id: roleId,
3. в user-row мы используем наш кастомный хук который принимает в пропсах session и params(userId и newUserRoleId)
4. в options мы создаём новую опцию updateUserRole(делаем на подобии как fetch-users) это асинхронная функция будет в которой мы принимаем(userSession, userId, newUserRoleId) проверяем есть ли права на это действие импортируем роль администратора в этой функции вызываем нашу функцию setUserRole(userId, newUserRoleId) с входными данными и return {error: null,res: true,};
5. в обработчике onRoleSave мы обязательн передаём наш хук в который предаём 'updateUserRole' далее обрабатываем с помощью then(() =>{setInitialRoleId(newUserRoleId);}) заводим новое сотояние в котором есть начальное состояние и меняем его сетфункцией в нашем обработчике onRoleSave и intialRoleId мы передаём в эту переменную const isSaveButtonDisabled = selectedRoleId === initialRoleId;
6. не забываем в server добавить нашу option(updateUSerRole) так же не забываем её индексировать
7. в users при фильтрации наешго массива roles мы должны в аргументах обязательно принимать ({ id: roleId }) => roleId !== ROLE.GUEST для того селектор нам не выдавал поле гость мы его передаём с помощью пропсов в компонент userRow

---

урок 8

1. реализовываем кнопку удаления пользователя
2. мы это будем делать в компоненте Users onUserRemove - обработчик для удаления так же используем наш кастомный хук requestServer который принимает операцию(removeUser и userId удаление пользователя)
3. создаём флаг const [shouldUpdateUserList, setShouldUpdateUserList] = useState(false);
   в обработчике мы делаем запрос на сервер принимаем данные и с помощью setShouldUpdateUserList
   делаем !shouldUpdateUserList так как наш флаг при нажатии будет меняться мы его можем закинуть в массив зависемостей useEffect для того чтобы перерэндаривалась страница
4. в компоненте users мы в пропсы userRow передаём такой проп onUserRemove={() => onUserRemove(id)}
5. в компоненте мы передаём ссылку в обработчик
6. далее мы уже начинаем рабоать с сервером(bff) в server передаём(removeUser)
7. в opiration мы создаём remove-user который реализован примерно как и остальные в нём проверяется администратор ты или нет он вызываем функцию delete-user(из api) индексируем это функцию
8. так же в api создаём запрос на удаление delete-user в ней мы запрашиваем по ручке пользователя и используем method: 'DELETE' не забываем индексировать
9. коммитим ветку наш функционал users реализоване который видит только администратор в будущем реализуем логику чтобы все остальные роли не видели кнопку users

---

---

---

Страница Статья

Урок 1(Базовая разметка)

1. в страницы добавляем папку post
2. её мы разделим на 2 основных компонента <PostContent> и <Comments> для них создаём папку components так же отдельные папки и индексируем их
3. будем исполльзовать redux импортируем useDispatch и useSelector
4. импортируем хук useParams и useEffect будем их использовать в компоненте Post(для того чтбы взять данные из адреса)

Урок 2(Работа с данными)

1. первым делом делаем load-post-async который будет диспатчить синхронные action в аргументах принимаем кастомеый хук и postId вызываем хук передаём операцию 'fetchPost' и postId когда данные придут вызовем диспатч отправим синхронный action с postData
2. создаём setPostData в котором мы принимаем postData и мы дожны вернуть объект у которого тип будет(подготавливаем action-type) export const setPostData = (postData) => ({
   type: ACTION_TYPE.SET_POST_DATA,(в видео забыли отправить)
   payload: postData,
   });
3. в асинхронном action мы импортируем синхронный action напрямую чтобы не было циклической зависимости
4. далее обрабатываем reducer в нём мы передаём action который только что создали он возвращает ...state и action.payload так же передаём initialState тот который мы прописывали изначально
5. в post мы импортируем наш loadPostAsync
6. добавим новый сектор для извления поста не забываем его индексировать
7. post мы передаём во внутрь наших дочерних элементов , в первую очередь передаим и отрисуем в компоненте PostContent в пропсах которого мы принимаем объект post={id, title, imageUrl, content, publishedAt} и уже делаем нашу верстку
8. далее создаём operation 'fetchPost' в котором мы делаем асинхронный запрос на сервер эта страница будет доступна всем ограничений не нужно делать тут мы возвращаем post
9. в хуке мы добавляем ещё одно исключение гдн не нужна будет сессия это будет fetchPost
10. далее в api создаём get-post в котором будет трансформер в этом компоненте мы берём postId в ручке меняем адрес делаем на подобии get-user только мы проверяем не массив а обычный объект
11. создаём транформер для get-post тут мы не делаем snake_case как в get-user потмоу что в базе данных нашей данные в camelCase и также индексируем его
12. далее преобразуем компонент server по сути это посредник между всеми opiration в index.js мы сделаем следующее
    import \* as server from './opirations';
    export { server };
    это значит что все файлы opiration мы берём собираем в один объект и экспортируем, файл server удаляем

---

урок 3(Страница Статья.Верстка)

1. зазнимаемся верской задаём в Post margin: 40px 0;padding: 0 80px; для целовго page чтоыб наши два компонента имели общие отступы
2. в компоненте post-content для картинки делаем & img {float: left;margin: 0 20px 10px 0;}
3. иконку, дату публикации, икноку редактировать и удалить помещаем в контеинер & .spesial-panel {display: flex;justify-content: space-between;margin: -20px 0 20px;}
4. для иконку календарь и да ты задаём другие стили & .published-at {display: flex;font-size: 18px;}
5. для 2х других иконок & .buttons {display: flex;} общие стили для того чтобы они были в столбик и в самих общих иконок бы задаём внутри размер и отступы;

---

урок 4(Блок коментарием.Базовая верстка)

1. первым делом создам компонент commetns внутри него textarea и иконка самолетика отправить
2. создаём локальное сотояние для textarea в onChange передаём callback с объектом в котором target и с помощью состояния меняем его setNewComment(target.value) value={newComment}
3. в компоненте Comments мы задаём стили относительно наших размеров общая ширина 580px display:flex обычные наши заготовки textarea и иконку задём ширину 100% вообщем располагаем написание и отправление нового коментраия по центру
4. далее в проекте создаём папку comment и реализуем его это будут наши коментарии все под статьёй в этом компоненте мы прокидываем пропсы id, author, publishedAt, content и делаем два блока в одном (1. author,иконка 2. publishedAt и иконка)в этом блоке justify-content: space-between во втором content задаём стили для тех блоков где иконки display:flex

---

урок 5(Блок коментариев. Функционал добавления)

1. будем реализовывать кнопку отправки комментария создаём её в onClick мы передаём callback в аргументы которого мы передаём postId(получаем в пропсах родительского элемента) userId(получаем через useSelector то есть берём его из redux store) и newComment(завоим состояиние в этом же компоненнте) на верхнем уровне
2. во вторую очередь реализовываем textarea в value передаём значение из нашего состояния newComment так же передаю placeholder и onChange в него передаём callback с setфункцией и меняем его с помощью target.value в аргументах извлекаем target
3. в useEffect диспатчим асихронный action в его аргументы передаём requestServer, postId, userId, content
4. создадим асинхронный action в замыканиях которого диспатч и передаём в него данные в нём мы уже вызываем синхронный action в аргументы которого мы предаём данные которые мы обрабатываем с помощью then
5. создаём ручку в api add-comment в котрой есть метод POST в header те же данные что и обычно а в тело передаём данные такие body: JSON.stringify({
   author_id: userId,post_id: postId,published_at: generateDate(),content,})не забываем индексировать новый компонент
6. создаём операцию add-post-comment в котором мы принимаем ссесию и данные для создания коментария к посту импортируем ROLE и session и с помощью этих компонентов проверяем доступ далее вызываем наш addComment с информацией о userId postId content
7. важный момент чтобы подгружались коментарии нам нужно создать ручку get-comments в кототором мы обращаемс к коментарих по postId в этом компоненте мы обрабатываем с помощью json
8. в fetchPost мы возвращаем коментарии и так же в add-post-comments делаем тоже самое чтобы при загрузки данных загружались комментарии
9. для того чтобы зарегестрироваться нужно быть авторизованным пользователем

---

урок 6 (Доработка входа. Сохранение сессии после перезагрузки страницы)

1. реализовываем в фитчу чтобы не было выхода при обновлении страницы sessionStoroge
2. sessionStoroge это встроенная функция в браузер в ней будем хранить наши данные при входе в в компоненте Blog мы используем useLayoutEffect который не ошидает отрисовки и срабатывает сразу в нём заводим переменную const currentUserDataJSON = sessionStorage.getItem('userData') проверяем если там ни чего нету ни чего и не делаем если там что то есть мы диспатчим эти данные с помощью setUser данные которые мы передаём , в sessionStoroge всё хранится в строках у нес единсвенные данные число это roleId и делаем следующее dispatch(setUser({
   ...currentUserData,
   roleId: Number(currentUserData.roleId),
   }),); тут мы реализовали чтобы данные не стирались
3. далее в authrization передаём sessionStorage.setItem('userData')
4. в registeration тоже самое sessionStorage.getItem('userData')
5. нужно преобразовать в Blog JSON обратно const currentUserData = JSON.parse(currentUserDataJSON); и уже новые данные диспачим выше с этим названием переменной
6. в нашей шапке там где у нас выход нужно удалять sessionStoroge
7. в компоненте control-panel мы реализуем выход и удаление sessionStorage.removeItem('userData');

---

урок 7 (Доработка входа. Исправление ошибок и добавление первого коммнтария.)

1. создаём в БД session
2. в api создаём файл add-session, get-session, delete-session индексируем их и будем использовать в session.js
3. так же создаём трансформер transform-session в нём делаем прямую обработку и индекируем
4. в sessions.js мы меняем. структуру файла с испотльзованием уже новых методов для session
5. во всех файлах в которых есть запрос на доступ мы меняем проверку доступности access в котором принимаем hash и accessRoles и если доступа нету выдаём ошибку , если всё ок двигаемся дальше так же во всех компонентах в аругментах принимаем hash
6. в нашей БД мы обязательно указываем sessions массивом(сессий может быть много) и в get-session .then(([loadedSession]) => loadedSession && transformSession(loadedSession))

---

урок 8(Страница Статья. Верстка комментариев)

1. стилизуем компонент для textarea и блока комментариев убираем display: flex
2. комментарии, иконку автора , имя автора, иконку колендарь и дату помешаем в обёртку(comment) и добавляем иконку удалить
3. обёртке(comment) добавляем рамку добавляем отступы и делаем ширину такую же как у textarea
4. т к у нас другие поля на сервере нам нужно трансформировать данные в getComments
5. создаём трансформер индексируем его
6. в getComments мапим трансформерные данные
7. в fetchPost мы делаем чуть сложнее логику чем нужно в этом компоненте мы запрашиваем comments по ручке и users по ручке , чтобы нам получить имя автора мы делаем слудующее const user = users.find(({ id }) => id === comment.authorId); получили объект user и возвращаем return {...comment,author: user?.login,};
8. следовательно в нашем компоненте передаём res: {...post,comments: commentsWithAuthor,}
9. в обработчике где мы добавляем комментарий мы обнуляем содержимыое textarea

---

урок 9(Удаление комментария)

1. в компоненте comment есть иконка удалить комментарий создаём обработчик в нём мы принимаем в аргументах id в этой функции мы диспачим данные в нём будет асинхронный action для удаления - removeCommentAsync
2. создаём асинхронный action копируем он будет похож на добавление комментария как обычно он возвращает callback в аргументах которого dispatch а в аргументах самого action у нас кастомный, хук id и postId для отрисовки данных
3. в компоненте мы принимаем наш кастомный хук который принимает операцию и какие то параметры и передаём его в аргументы
4. далее создаём операцию remove-post-comment , будет похожа на добавление так же указываем что у нас может удалять админ и модератор, тут вызываем deleteComment и передаём id
5. postId нам нужно передать в action принимаем мыего и прекидываем через пропсы из comments
6. далее создаём ручку delete-comment будет похожа на delete-session подправляем названия и индексируем

---

урок 10 (Модальное окно. Подготовка и стилизация)

1. в компонентах создаём отдельный компонент Model в нём возвращаем разметку нашего модельного окна в котором есть 2 обёртки и в них уже текст(заголовок с вопросом) и блок кнопок(да и отмена)
2. в кнопки доавляем обработчики(onConfirm и onCancel)
3. само модельное окно мы будем брать из состояния, в app-reducer добавляем в начальное состояние объект model в нём флаг откыто ли модельное окно, текст для модельноего окна и 2 функции
4. создаём селекторы для этих данных(4 селектора) индексируем их и импортируем их в наш компонент Model
5. само модульное окно индексируем и добавляем в компонент Blog и вызываем его после Footer(не важно какое место он будет занмать, мы его стилизуем так чтобы он поверх всего)
6. начинаем стилизовать (position:fixed и z-index) - для того чтобы окно было поверх всего при задавании всех сторон(top,bottom,right,left:0) моделное окно растянется на весь экран
7. мы делалли обёртку специально для того чтобы затемнить фон & .overlay {position: absolute;width: 100%;height: 100%;background-color: rgba(0, 0, 0, 0.7);}
8. в самом модельном окне & .box мы задаём отступы, центровку, рамку, и размеры чтобы всё было по центру и указываем чтобы текст был по центру
9. для самих кнопок & .buttons {display: flex;justify-content: center;} указываем чтобы они были по центру друг за другом в колонку

---

урок 11(Модальное окно. Функционал удаления)

1. в компоненте comment мы в обработчике удаления мы диспатчим нашу модалку с action в которых есть 2 action один на закрытие(CLOSE_MODAL) другой на открытие(openModal)
2. создаём первый на action CLOSE_MODAL так же доавляем его в action-type в этом action мало нагрузочных данных поэтому мы указываем этот action в CAMEL_CASE
3. далее создаём ещё один action openModal в котором мы принимаем modalParams
   export const openModal = (modalParams) => ({
   type: ACTION_TYPE.OPEN_MODAL,
   payload: modalParams,
   }); два наших новых action индексируем
4. в reducer мы разворачиваем и обрабатываем данные для закрытия и открытия
5. для закрытия мы передаёи изначальное состояние
6. для открытия return {...state,modal: {...state.modal,...action.payload,isOpen: true,},} то есть мы разворачиваем наше состояние потому что кроме модального окна есть ещё данные и уже в объекте modal мы разварочиваем и состояние модалки, передаём action и isOpen: false
7. в самом компоненте мы уже дорабатываем обработчики и подставлем text

---

урок 12(Редактирование статьи. Подготовка редактора)

1. в роутинг ниже компонента по адресу /post/:id вставляем ещё один компонент с адресом /post/:id/edit реадактирование
2. создаём компонент PostForm(копируем PostContent) в нём мы меняем поля где картинка и заголовок на Input с не управляемыми полями
3. в компоненте Post используем хук useMatch присваиваем его к пере енной isEditing если реадактируем то выводим маршрут на страницу редактировать, если нет на пост где можно добавить комментарий
4. чтобы мы могли редактировать наш большой текст в div передаём атрибут contentEditable={true} и suppressContentEditableWarning={true} - для того чтобы не было предупреждений от реакта что он не может это контролировать
5. в нашем тексте вместо <br><br> мы используем \n\n - разделление на абзац в content в стилях нужно добавить white-space: pre-line; текст будет прилично теперь оформлен
6. часть special-panel у нас получается как выносимый компонент его можно переиспользовать и также назовём в нём будет будет две иконки одна колендарь с датой в которой будет в пропсах publishedAt и перенесём все стили которые касались special-panel и его дочерних элентов, единственное что будет отличатся это 2 Icon одна будет сохранить(дискета) другая(редактировать)
7. кнопку которая будет отличатся её можно прокинуть в качестве пропсов editButton
8. в наших компонентах(PostForm и PostContent) теперь будет общий элемент SpecialPanel с пропсами дата публикации, отступ сверху(margin) и editButton(Icon целый компонент)
9. в PostForm добавим useRef на элементы которые мы редактируем imageUrl,title и content
10. создаём обработчик const onSave = () => {
    const newImageUrl = imageRef.current.value;
    const newTitleeUrl = titleRef.current.value;
    const newContentUrl = sanitizeContent(contentRef.current.innerHTML);
    }; в этом обработчике мы отправляем актульные значения элементов которые мы редактируем или можем редактировать, залогируемся и при изменении мы в консоли получим актуальыне значения элеентов редактирования.
11. в этом компоненте мы используем утилиту которая будет заменять часть кодовых строк которые мы используем на бэке, грубо говоря мы меняем часть кода на другие элементы,но в моём случае у меня абзацы переносятся хорошо и вопросов не возникает , это фитчу можем использовать она ни как не влияет на логику

---

урок 13(Редактирование статьи. Функционал сохранения)

1. нам нужно отправить данные которые мы уже отредактировали в обработчике onSave через dispatch мы отпраляем асинхронный action в котором передаём наш requestServer и данные которые мы обновили, так же передаём id для того чтобы потом перейти на страницу then(() => navigate(`/post/${id}`)), импортруем хуки useDispatch и useNavigate
2. далее начинаем создавать асинхронный action(savePostAcync) в нём передаём в первом аргументе передаём requestServer и newPostData, в аргументах callback dispatch обрабатываем
3. dispatch можно обработать как Promise и в обработчике мы это делаем в выводом на страицу then(() => navigate(`/post/${id}`))(редирект на страицу уже обычного поста) это сила асинхронного action
4. подготавляиваем операцию, в ней мы принимаем hash и newPostData проверяем доступ и обращаемся по ручке в серверу и далее взвращаем что нету ошибок и updatePost(обновленные данные) индексируем компонент и импортруем из асинхронного action
5. в api создаём запрос updatePost в этом компоненте мы принимаем все данные которые можно редактировать и передаём id чтобы мы могли обратится именно к нужному посту по id в самом компоненте мы возвращаем(imageUrl,title,content) индексируем и используем в operation
6. в компоненте postContent мы на иконку редактирования перейти на страницу редактировнаия поста onClick={() => navigate(`/post/${id}/edit`)} id принимаем в аргументах
7. в асинхронном action мы диспатчим синхронный action setPostData(postData.res) - уже с данными с сервера

---

урок 14(Удаление статьи)

1. в компонент specialPost мы копирем обработчик из comment немного модэрнизируем под удаление поста и передаём обработчик в onClick компонента где мы хотим удалить
2. импортруем все компоненты котоые нам потребуются

3. реализуем в Icon в пропсах передаём onClick и принимаем в div и в описании компонента мы делаем условный редаринг &:hover {cursor: ${({ onClick }) => (onClick ? 'pointer' : 'default')};} нужно для того чтобы там где у нас на икнонках не висит обработчик не показывалось что можно кликнуть

4. в postForm и PostContent мы передаём id в пропсах для того чтобы по этому пропсу будем удалять статью
5. создаём action removePostAsync будет похож на removeCommentAsync копируем его меняем название, то что передаём в requestServer
6. в specialPanel после removePostAsync делаем редирект на главную страицу('/') то есть наш dispatch может рабоать как Promise
7. делаем операцию removePost(копируем из удаления комментария) делаем проверку кто может удалять и вызывваем(deletePost-ручка с сервера)
8. создаём api deletePost(копируем любое удалние) индексируем

9. в компоненте removePost вызываем все комментарии с помощью getComments и с помощью этого компонента мы прохлдимся по всем элементам массива, то есть комментариям с этим postId aweit Promise.all(comments.map(({ id: commentId }) => deleteComment(commentId)));загрузка поста без комментариев
10. в компоненте post мы заводим useLayoutEffect синхронный action его мы будем вызывать до данных с сервера , в него мы передаём синхронный action который будет сбрасыввать состояние , а за ним будет вызыввать обычный useEffect синхронный
11. заводим синхронный action в action-type передаём его
12. в post-reducer мы обрабатываем наш action(возвращаем initilState)

---

урок 15 (Страница Новая статья. Создание статьи)

1. для начала мы исправим чтобы при наведении на кнопки курсор превращался в палец, в компоненте Icon мы в стили добавляем cursor: ${({ inactive }) => (inactive ? 'default' : 'pointer')}; и inactive передаём в иконки где наведение с пальцем не нужно(календарь 2 раза и автор)

2. в роутинге для новой статьи мы передаём <Post> он будет похож на старицу редактирования
3. в компоненте Post мы создлаём ещё один флаг isCreating через хук useMatch
4. в useEffect мы делаем проверку если это isCreating дальше не идём
5. в разметке условного редаринга мы добаляем isEditing || isCreating и тогда выводим PostForm
6. далее обращаемся в операцию savePost и const savedPost =
   newPostData.id === ''
   ? await addPost(newPostData)
   : await updatePost(newPostData); проверяем если id пустая строка то это создание(addPost) если наоборот(updatePost) проверка в компоненте до этих строчек остаётся такая же, логика что это может делать администратор не меняется и res: передаём savedPost
7. в api создаём ручку(addPost) для добавления данных на сервер(копируем добавление комментария и подставляем данные которые нам нужны) обрабатываем под формат json и индексируем и добавляем в savePost
8. в поле где мы создаём текст а точнее post-form(стилизуем) добаляем минимальную высоту и рамку для того чтобы было видно куда печатать
9. в post-form нужно убрать иконку календаря и корзики мы это делаем с помощью publishedAt && и далее что нужно отобразить(если есть дата публикации мы выводим эти кнопки, если нет мы создаём пост)
10. в postForm кнопка сохранения без отступов(не передавать margin)

---

урок 16 (Страница Новая статья. Доработки страницы)

1. в компоненте post в uLE в массив зависемостей передаём флаг(isCreating) а в uE мы делаем провреку if(isCreating){return}

2. самый простой вариант сбросить форму импортировать из postReducer initialState и передать в PostForm такую запись isCreating ? initialState : post

3. пойдём по другому пути в нашем компоненте заменим инпуты в которых у нас imageUrl и title(они неуправляемые) мы меняем их на управляемые меняем uR на uS и первые елементы в массивах передаём в value инпутов , также делаем для инпутов onChange извлекаем target и setаем
4. для того чтобы у нас поля очистились useLayoutEffect(() => {
   setImageIrlValue(imageUrl);
   setTitleValue(title);
   }, [imageUrl, title]); мы передаём этот хук с полями инпутов
5. после этого нам нужно обратить внимание на редирект после отправления/сохранения(then(({ id }) => navigate(`/post/${id}`))) извлекаем id из данных которые мы получили
6. в асинхронном action нужно вернуть return updatedPost.res; для того чтобы данные вернулись

---

---

---

Главная страница

урок 1 (Получение статей)

1. создаём ещё компонент main(вывод всех статей) добавляем его в основной роутинг(blog.jsx)
2. в нём мы будем использовать useEffect для загрузки всех постов(загрузку постов будем загружать один раз) в этом хуке мы используем кастомный хук и будем передавать fetchPost(операция)
3. т к у нас список статей не будет хранится локально(мы его не используем больше ни где) мы создаём состояние posts(изначально пустой массив)данные мы setаем в useEffect
4. а в разметку передаём posts и делаем map(мы в аргументах принимаем данные которые нам нужны для отрисовки)
5. и возвращать этот массив будет уже компонент(PostCard) с пропсами
6. создаем и индексируем компонент PostCard и принимаем в пропсах всё что мы передали
7. создаём операцию fetchPosts
8. создаём запрос getPosts - запрашиваем все посты в нём используем трансформер который у нас уже есть не забываем индексировать
9. в fetchPosts мы проверку не делаем т к эту страницу могут видеть все, этом компоненте const [posts, comments] = await Promise.all([getPosts(), getComments()]) с помощью данной записи мы возвращаем посты и комментарии
10. т к мы получаем комментарии по id , а нам нужно получить все комментарии мы самом запросе делаем условный рендаринг если postId === undifined то мы возвращаем все статьи , ну и наоборот и ещё добавляем postId
11. чтобы нам получить количество комментариев поста (вынесем его отдельно в утилиту) мы берём все комментарии и фильтруем по PostId и в утилите возвращаем уже длинну массива
12. в самом компоненте мы разворачиваем пост и добавляем commentCount
13. в кастомном хуке добавляем наш fetchPosts для того что эта операия не использует hash

---

урок 2 (Верстка)

1. начинаем верстать основную страинцу(так же передаём картинку)
2. так как у нас на главной странице будут не только посты мы наш массив помещаем в обёртку и стилизуем его & .post-list {display: flex;flex-wrap: wrap;padding: 20px;} с переносом в правую сторону с переносом и отступом
3. далее работаем с PostCard первым делом мы весь компонент помещаем в ссылку импортруем Link и оборачиваем и указываем адрес поста куда мы хотим перейти
4. в компоненте PostCard мы уже отдельно для каждого элемента мы задаём стили точечно

---

урок 3(Пагинация. Когда стоит внедрять доп. зависимости в проект)

коротко о пагинации(возможность переноса контента на какое то количество страниц заданного в настройках панигации)
нужно помнить

1. укатуальность данного инструмента
2. стоит ли использовать дял коммерческого проекта
3. простата в коде(бандл приложения)

---

урок 4 (Пагинация. Основной функционал)

1. в getPosts мы дополняем наш url page и limit(кол-во статей на странице)
2. в fetchPosts аргументы вызова Posts мы добавляем page и limit и в пропсах самой операции их принимаем
3. в main вместе с хуком передаём эти параметры
4. page берём из состояния начальное значение будет 1
5. limit будет константой это значение помтоянное( в аншем проекте это 9) импортируем это в main
6. page передаём в массив зависимостей useEffect
7. начинаем делать верстку в ней будет 4 кнопки(в начало,предыдущая,следующая,в конец) и по середине нашей кнопок будет информация на какой странице мы сейчас находимся
8. далее стилизуем наши кнопки и контент по середине и setаем на нужные нам страницы и задаём disabled(в наших кнопках если страница 1(первые 2 кнопки не доступны))

---

урок 5 (Пагинация. Финальные доработки и исправления ошибок)

1. реализовываем чтобы мы находили последнюю страницу(last)
2. в main заводим состояние(lastPage) передаём его в пропсы Pagination(в нём передаём куда нам требуется и disabled если lastPage ) а сет функцию передаём в useEffect
3. в компоненте main мы делаем проверку если lastPage > 1 контент пагинации выводим
4. в getPosts мы получаем заголовки с сылкой , т к у нас теперь два ответа мы используем Promise.all и добавляем такую запись loadedposts.headers.get('Link') и возвращаем уже .then(([loadedposts, links]) => ({posts: loadedposts && loadedposts.map(transformPost),links,})) посты через трансформер и ссылки
5. в fetchPosts диструктуризируем уже в объект с данными в этом компоненте мы возвращаем ответ первый это наш ответ с posts а второй с links
6. далее принимаем всё это дело в main
7. делаем утииту в которой мы мы будет возвращать результат который мы хотим индексируем компонент и переходим в main
8. в main мы принимаем данные объектом then(({ res: { posts, links } }) => {setPosts(posts);setLastPage(getLastPageFromLinks(links))},) вторая сет-функция должна возвращать утилиту с сылкой в аргументах(это будет число)

---

урок 6(Поиск статей. debounce())

1. в main создаём компонент Search выше списка статей в неём будет Input и Icon стилизуем их так чтобы иконка была в инпуте и текст не заходил на инпут(главное для всего компонента мы задаём position: relative а для иконки position:absolute чтобы иконка была сверху) далешь подгоняем размеры
2. начинаем рабоать над функционалом поиск по заголовку статьи
3. создаём состояние shouldSearch это будет флаг на поиск по фразе изначально он false в компонент Search передаём обработчик onChange в этом обработчике setSearchPhrase(target.value)
4. задаём новое состояние для фразы(serchPhrase) и передаём его в value Input в компоненте search так же в качестве пропсов передаём и onChange
5. searchPhrase передаём в аргументы requestServer
6. в fetchPosts(opiration) в аргументы передаём ещё searchPhrase => в getPosts вызова тоже => в самом getPosts тоже его принимаем => в url мы добавляем title_like=${searchPhrase}
7. создаём утилиту debounce() в ней так же вызываем clearTimeout(функция которая работает на отмену предыдущего таймаута если вызвали ещё один в течении переданного времени)
8. const startDelayedSearch = useMemo(() => debounce(setShouldSearch, 2000), []); для того чтобы не было рендаров при каждой смене флага(ссылка на функцию не будет менятся ) и в onChange передаём уже const onSearch = ({ target }) => {setSearchPhrase(target.value);startDelayedSearch(!shouldSearch);};
9. в разметке указываем если posts.lenght ? мы рендарим все посты : указываем 'статьи не найдены' и стилизуем его
10. до конца стилизовали поиск статей и верстку для пагинации.

---

---

---

Старинцы ошибок. Финальные Доработки.

урок 1 (Страница с ошибкой. Страница не существует)

1. проверяем ошибку по всем адрессам путём добавления не коректных символов к адрессам
2. в компоненте Post работая с асинхронным action мы можем обработать то что мы принимаем через then и далее передаём наши данные с сервера (dispatch возвращает Promise)
3. в асинхронном action если данные пришли то диспатчим их с помощью синхронного action и уже возвращаем postData
4. в компоненте Post мы используем наше состояние(если error есть то выводим ошибку если нет выводим разметку)
5. ошибки(400 это всё зарезолвенные промисы и в getPost наш случай мы обработаем этот момент) проверяем если res.ok возвращаем res
6. если const error =res.status === 404
   ? 'Такой страницы не существует': 'Что-то пошло не так. Попробуйте позднее.';
   return Promise.reject(error); мы обрабатываем ошибки на сервере и также отправляем через reject
7. в fetchPost мы уже обрабатываем через try/catch ошибки проверяем если есть ошибка то возвращаем ошибку и res: null,
8. в начальное состояние мы передаём true-в разметке не показывается
9. сделаем отдельный компонент error чтобы мы могли его переиспользовать подключаем этот компонент(content,post,blog)
10. добавляем флаг isLoading синхронная операция сразу true и возвращает null(пустую страницу) перед разметкой проверим с помощью if, а убирать её будем после того как данные пришли в useEffect
11. вынесем в константу текст ошибки и будем использовать в blog первым делом передаём
12. у нас ошибки будут и с сервера и с бэкенда(чем меньше запросов на сервер тем лучше)
13. в blog мы указываем константу - фронтенд ошибка , в post у нас ошибка с бэкенда

---

урок 2 (Страница с ошибкой. Доступ запрещён)

1.
